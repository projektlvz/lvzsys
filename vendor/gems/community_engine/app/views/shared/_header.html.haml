-# .navbar-toggle is used as the toggle for when the responsive design gets narrow and the navbar goes away
.navbar-header
  %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", type: "button"}
    %span.sr-only Toggle navigation
    %span.icon-bar
    %span.icon-bar
    %span.icon-bar
  = link_to configatron.community_name, home_path, :class => 'navbar-brand'
.navbar-collapse.collapse
  %ul.nav.navbar-nav
    = render 'categories/menu_item'
    = render 'clippings/menu_item'
    = render 'events/menu_item'
    = render 'forums/menu_item'
    = render 'posts/menu_item'
    = render 'users/menu_item'
    = form_tag("/users", method: "get", class: "navbar-form navbar-left") do
      .form-group
        = text_field_tag(:q, nil, placeholder: :search_stores_by_name_and_feature.l, class:"form-control")
        = submit_tag(:search.l, class: "btn btn-default")
    - if @header_tabs.any?
      - for tab in @header_tabs
        %li= link_to tab.name, tab.url
  %ul.nav.navbar-nav.pull-right
    = render 'messages/menu_item'
    = render 'sessions/menu_item'
