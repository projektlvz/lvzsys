- if (current_page?(users_path) || (params[:controller] == 'users' && !@user.nil? && @user != current_user)) && !@customers
  - css_class = 'active'
- else
  - css_class = 'inactive'

%li{:class => css_class}= link_to :shops.l, users_path